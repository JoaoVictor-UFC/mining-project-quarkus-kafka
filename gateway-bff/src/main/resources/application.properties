quarkus.http.port=8085

# Keycloak security configs
quarkus.oidc.auth-server-url=http://localhost:8080/realms/quarkus
quarkus.oidc.client-id=backend-service
quarkus.oidc.credentials.secret=secret

quarkus.oidc-client.auth-server-url=${quarkus.oidc.auth-server-url}
quarkus.oidc-client.client-id=${quarkus.oidc.client-id}
quarkus.oidc-client.credentials.secret=${quarkus.oidc.credentials.secret}
quarkus.oidc-client.grant.type=password

# Rest Client Configuration
org.br.mining.client.ProposalRestClient/mp-rest/url=http://localhost:8091
org.br.mining.client.ReportRestClient/mp-rest/url=http://localhost:8092

#Swagger-ui OpenApi
quarkus.swagger-ui.always-include = true
quarkus.smallrye-openapi.info-description= Mining Project Back-End For Front-End (BFF) - API